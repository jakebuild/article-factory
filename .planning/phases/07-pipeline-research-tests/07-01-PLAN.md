---
phase: 07-pipeline-research-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/article_factory/scheduler.py
  - tests/test_scheduler.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
must_haves:
  truths:
    - "run_pipeline_async creates a persisted task record and starts a detached worker subprocess without blocking"
    - "_execute_pipeline advances stages in documented order from NOTEBOOK_CREATED through COMPLETED when dependencies succeed"
    - "If any pipeline stage fails, task status becomes FAILED and error text is persisted"
    - "Retry behavior increments topic retry_count, re-queues retryable failures, and stops re-queueing once max retries are reached"
  artifacts:
    - path: "tests/test_scheduler.py"
      provides: "PIPE-01..PIPE-04 coverage for async pipeline scheduling, stage order, failure handling, and retry behavior"
      min_lines: 150
    - path: "src/article_factory/scheduler.py"
      provides: "Retry-aware failure path used by _execute_pipeline with explicit max-retry guard"
      contains: "increment_retry"
  key_links:
    - from: "tests/test_scheduler.py"
      to: "src/article_factory/scheduler.py"
      via: "run_pipeline_async and _execute_pipeline invocation with monkeypatched stage dependencies"
      pattern: "run_pipeline_async|_execute_pipeline"
    - from: "tests/test_scheduler.py"
      to: "subprocess.Popen"
      via: "mocked detached worker spawn assertions"
      pattern: "Popen"
    - from: "src/article_factory/scheduler.py"
      to: "src/article_factory/database.py"
      via: "retry increment and topic status re-queue wiring on failure"
      pattern: "increment_retry|update_status"
---

<objective>
Verify async scheduler behavior end-to-end under fully mocked dependencies, including detached process spawning, deterministic stage progression, failure recording, and retry/re-queue handling.

Purpose: Pipeline reliability is the core execution path; Phase 7 must prove it works offline before Phase 8 builds additional coverage on top.
Output: `tests/test_scheduler.py` covering PIPE-01..PIPE-04 and any minimal scheduler retry-path updates required for those tests to pass.
</objective>

<execution_context>
@/Users/giangnguyen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/giangnguyen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-async-pipeline/04-scheduler-SUMMARY.md
@.planning/phases/05-test-infrastructure/05-02-SUMMARY.md
@.planning/phases/06-database-errors-tests/06-01-SUMMARY.md
@src/article_factory/scheduler.py
@src/article_factory/models.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PIPE-01 test for task creation + detached subprocess spawn</name>
  <files>tests/test_scheduler.py</files>
  <action>
    Create `tests/test_scheduler.py` and add a focused test for `run_pipeline_async` that verifies both required effects from PIPE-01.

    1. Use fixture-backed in-memory DB path from `tests/conftest.py`; create a real topic row via DB helpers so task persistence is exercised.
    2. Mock `subprocess.Popen` (do not allow real process start) and call `run_pipeline_async(topic_id, ...)`.
    3. Assert returned `task_id` is non-empty UUID-like string and `get_task(task_id)` returns persisted task metadata for the same topic.
    4. Assert `Popen` called exactly once with:
       - command: `[sys.executable, "-m", "article_factory.main", "worker", task_id]`
       - `start_new_session=True`
       - `stdout=subprocess.DEVNULL` and `stderr=subprocess.DEVNULL`.

    Keep test offline and deterministic: no real subprocess and no real NotebookLM calls.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_scheduler.py -k "pipe_01 or run_pipeline_async" -v` exits 0.
  </verify>
  <done>
    A passing test proves `run_pipeline_async` persists a task and requests detached worker spawn with expected arguments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PIPE-02 and PIPE-03 tests for stage order and failed-stage recording</name>
  <files>tests/test_scheduler.py</files>
  <action>
    Extend `tests/test_scheduler.py` with async `_execute_pipeline` tests that mock stage dependencies and capture progress transitions.

    1. Monkeypatch stage collaborators (`create_notebook_for_topic`, `trigger_deep_research`, `wait_for_research_completion`, `generate_synthesis`, `generate_article`, `generate_infographic`, `generate_audio_briefing`, `export_all_artifacts`) so execution stays offline.
    2. Record `update_task_progress` calls and assert the stage order is exactly:
       `NOTEBOOK_CREATED -> RESEARCH_TRIGGERED -> RESEARCH_COMPLETED -> SYNTHESIS_DONE -> ARTICLE_DONE (when prompt/prompt_file provided) -> MEDIA_DONE -> COMPLETED`.
    3. Add a failure-path test where one stage raises (for example infographic generation), then assert task transitions to `FAILED` and persisted `error_message` contains the thrown error text.

    Reuse established async pattern from Phase 6 tests: `pytest.mark.asyncio`, deterministic monkeypatching, and no network.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_scheduler.py -k "pipe_02 or pipe_03 or execute_pipeline" -v` exits 0.
  </verify>
  <done>
    Passing tests prove documented stage ordering and failed-stage persistence behavior in `_execute_pipeline`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement and verify PIPE-04 retry/re-queue behavior with max-retry stop</name>
  <files>src/article_factory/scheduler.py, tests/test_scheduler.py</files>
  <action>
    Add retry-aware failure handling and cover it with tests.

    1. In `src/article_factory/scheduler.py`, enhance pipeline exception handling so topic retries are tracked through DB helpers:
       - increment topic retry count on pipeline failure,
       - re-queue topic to `PENDING` while retry count is below max,
       - stop re-queueing after max retries and keep terminal failed state.
    2. Keep task-level behavior from PIPE-03 intact (`TaskStatus.FAILED` + error recording) while adding topic-level retry logic.
    3. Add tests that assert both branches:
       - below max retries: retry count increments and topic is re-queued,
       - at max retries: retry increments to cap (or remains capped, per implementation) and topic is not re-queued again.

    Use existing model constraints (`Topic.can_retry` / retry_count semantics) and do not introduce real waits or subprocess calls.
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/test_scheduler.py -k "pipe_04 or retry" -v` exits 0.
    2. `.venv/bin/python -m pytest tests/test_scheduler.py -v` exits 0.
  </verify>
  <done>
    PIPE-04 is covered by passing tests and scheduler failure path enforces retry increment + bounded re-queue behavior.
  </done>
</task>

</tasks>

<verification>
1. `tests/test_scheduler.py` exists and maps PIPE-01..PIPE-04 explicitly.
2. `run_pipeline_async` test asserts detached subprocess arguments and persisted task record.
3. `_execute_pipeline` tests assert both ordered success stages and failed-stage persistence.
4. Retry tests assert bounded re-queue semantics with no real API calls or subprocess execution.
</verification>

<success_criteria>
- Scheduler test suite proves async pipeline orchestration behavior entirely offline.
- PIPE-01..PIPE-04 each have explicit, passing assertions in `tests/test_scheduler.py`.
- Retry handling is deterministic and bounded by max-retry semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pipeline-research-tests/07-01-SUMMARY.md`
</output>
