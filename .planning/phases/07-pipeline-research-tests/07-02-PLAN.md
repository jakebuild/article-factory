---
phase: 07-pipeline-research-tests
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/article_factory/research.py
  - tests/test_research.py
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
  - RES-04
must_haves:
  truths:
    - "run_research starts research, polls to completion, and imports discovered sources using mocked client paths"
    - "Research polling raises TimeoutError when elapsed duration exceeds configured timeout without completion"
    - "generate_synthesis returns string content (not a file path)"
    - "Synthesis output includes both discovered source list and a research summary section"
  artifacts:
    - path: "tests/test_research.py"
      provides: "RES-01..RES-04 async research orchestration and synthesis output coverage"
      min_lines: 130
    - path: "src/article_factory/research.py"
      provides: "Research orchestration wiring compatible with source import and deterministic timeout behavior"
      contains: "run_research"
  key_links:
    - from: "tests/test_research.py"
      to: "src/article_factory/research.py"
      via: "async tests calling run_research, poll_research, and generate_synthesis with monkeypatched collaborators"
      pattern: "run_research|poll_research|generate_synthesis"
    - from: "src/article_factory/research.py"
      to: "src/article_factory/notebook_lm.py"
      via: "NotebookLMClientWrapper methods: create_notebook/start_research/poll_research/import_sources/list_sources"
      pattern: "NotebookLMClientWrapper|import_sources|list_sources"
    - from: "tests/test_research.py"
      to: "tests/conftest.py"
      via: "fixture-backed offline execution and shared async mock conventions"
      pattern: "mock_nlm_client|monkeypatch"
---

<objective>
Add offline async tests for research orchestration and synthesis generation, and make minimal research-module adjustments needed so behavior aligns with Phase 7 requirements.

Purpose: Research is a critical dependency for pipeline correctness; these tests must prove orchestration behavior without external API credentials.
Output: `tests/test_research.py` covering RES-01..RES-04 plus any targeted `research.py` fixes required to satisfy those tests.
</objective>

<execution_context>
@/Users/giangnguyen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/giangnguyen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-layer/02-research-SUMMARY.md
@.planning/phases/05-test-infrastructure/05-02-SUMMARY.md
@.planning/phases/06-database-errors-tests/06-02-SUMMARY.md
@src/article_factory/research.py
@src/article_factory/notebook_lm.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RES-01 orchestration test and align run_research with source-import requirement</name>
  <files>src/article_factory/research.py, tests/test_research.py</files>
  <action>
    Create `tests/test_research.py` and implement RES-01 coverage around full `run_research` orchestration with mocked wrapper/database calls.

    1. Mock research-path collaborators so test stays offline: `get_topic`, `set_notebook_id`, `update_status`, `increment_retry`, and `NotebookLMClientWrapper` async methods.
    2. Assert `run_research(topic_id)` performs the sequence: start deep research -> poll until completed -> import discovered sources into notebook.
    3. If current `research.py` flow does not import discovered sources from `run_research`, add minimal production wiring to satisfy RES-01 (preserve existing status transitions and error handling).
    4. Assert final result includes completed status and imported/discovered source information consistent with implementation.

    Follow locked scope decision: no real NotebookLM API calls; all research interactions are mocked.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_research.py -k "res_01 or run_research" -v` exits 0.
  </verify>
  <done>
    A passing RES-01 test proves run_research orchestration covers start, polling, and source import behavior entirely through mocks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add RES-02 timeout test for poll_research max-duration behavior</name>
  <files>tests/test_research.py</files>
  <action>
    Add deterministic timeout coverage for `poll_research`.

    1. Monkeypatch time progression (`asyncio.get_event_loop().time`) or use controlled side effects so elapsed time exceeds timeout quickly without long sleeps.
    2. Mock poll status to remain `in_progress` and assert `TimeoutError` is raised when timeout threshold is exceeded.
    3. Assert timeout path does not attempt real network calls and keeps retry/status side effects consistent with current module behavior.

    Reuse Phase 6 async testing conventions: small timeouts, deterministic event-loop control, no flaky wall-clock waits.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_research.py -k "res_02 or timeout or poll_research" -v` exits 0.
  </verify>
  <done>
    A passing test proves `poll_research` raises TimeoutError when completion never occurs before configured max duration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add RES-03 and RES-04 synthesis content assertions</name>
  <files>src/article_factory/research.py, tests/test_research.py</files>
  <action>
    Add synthesis output tests and make small compatibility fixes if needed.

    1. Test `generate_synthesis(notebook_id, topic_slug)` returns a string object (not path-like output) for RES-03.
    2. Assert returned content includes:
       - discovered sources section,
       - at least one discovered source entry,
       - research summary heading and summary body text (RES-04).
    3. If synthesis source rendering fails for fixture-returned source shapes (dict vs object), patch `research.py` to support both while preserving existing output structure.
    4. Run full research test module after updates.
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/test_research.py -k "res_03 or res_04 or synthesis" -v` exits 0.
    2. `.venv/bin/python -m pytest tests/test_research.py -v` exits 0.
  </verify>
  <done>
    Passing tests prove synthesis returns string content with discovered sources and research summary sections.
  </done>
</task>

</tasks>

<verification>
1. `tests/test_research.py` exists and maps RES-01..RES-04 to explicit test cases.
2. `run_research` orchestration test verifies mocked start/poll/import flow.
3. Timeout test is deterministic and fast (no long sleep waits).
4. Synthesis tests assert string output plus required discovered sources and summary sections.
</verification>

<success_criteria>
- Research orchestration behavior is validated end-to-end in offline async tests.
- RES-01..RES-04 each have direct, passing assertions.
- Any required `research.py` changes remain minimal and test-driven.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pipeline-research-tests/07-02-SUMMARY.md`
</output>
