---
phase: 06-database-errors-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_database.py
autonomous: true
must_haves:
  truths:
    - "A topic created via database helpers can be read back by ID with topic, prompt, and status intact"
    - "Status transitions through NEW -> PENDING -> PROCESSING -> COMPLETED and PROCESSING -> FAILED persist and read back correctly"
    - "Looking up an unknown topic ID returns None without raising exceptions"
    - "Concurrent async database operations complete in test runs without sqlite locking errors"
  artifacts:
    - path: "tests/test_database.py"
      provides: "Database CRUD, transition, unknown-id, and concurrent-operation coverage for DB-01..DB-04"
      min_lines: 120
    - path: "tests/conftest.py"
      provides: "In-memory SQLite fixture configuration compatible with concurrent async DB test execution"
      contains: "db_session"
  key_links:
    - from: "tests/test_database.py"
      to: "src/article_factory/database.py"
      via: "create_topic/get_topic/update_status calls"
      pattern: "create_topic|get_topic|update_status"
    - from: "tests/test_database.py"
      to: "tests/conftest.py"
      via: "autouse fixture-backed in-memory SessionLocal patching"
      pattern: "db_session|monkeypatch"
    - from: "tests/test_database.py"
      to: "src/article_factory/models.py"
      via: "TopicStatus transition assertions"
      pattern: "TopicStatus\\.(NEW|PENDING|PROCESSING|COMPLETED|FAILED)"
---

<objective>
Add focused database-layer tests for CRUD behavior, status transitions, unknown-topic lookup, and concurrent async operations using the shared in-memory fixture path.

Purpose: Phase 6 must validate persistence and state-machine behavior without touching runtime production flows or real files.
Output: `tests/test_database.py` with DB-01..DB-04 coverage and fixture adjustments (if required) in `tests/conftest.py` to keep concurrent test execution deterministic.
</objective>

<execution_context>
@/Users/giangnguyen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/giangnguyen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-infrastructure/05-01-SUMMARY.md
@.planning/phases/05-test-infrastructure/05-02-SUMMARY.md
@src/article_factory/database.py
@src/article_factory/models.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden DB fixture for concurrent async test stability</name>
  <files>tests/conftest.py</files>
  <action>
    Keep the existing fixture-driven in-memory isolation pattern from Phase 5, and update `db_session` only as needed so concurrent async DB tests are reliable.

    1. Preserve monkeypatching of `article_factory.database` globals (`_engine`, `SessionLocal`) as the source of DB isolation truth.
    2. If concurrent tests hit in-memory sqlite thread/connection issues, configure the test engine for cross-task consistency (for example StaticPool/shared in-memory settings) while keeping it in-memory only.
    3. Do not introduce file-backed sqlite or mutate runtime `src/article_factory/database.py` behavior.

    Why: DB-04 requires concurrent async operations in tests; fixture setup must support this without violating the locked isolation decision.
  </action>
  <verify>
    1. `tests/conftest.py` still patches both `_engine` and `SessionLocal`.
    2. `.venv/bin/python -m pytest tests/test_models.py -v` passes after fixture adjustment.
  </verify>
  <done>
    The shared DB fixture remains autouse-compatible, in-memory, and stable for concurrent async DB test execution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add database behavior tests for DB-01, DB-02, and DB-03</name>
  <files>tests/test_database.py</files>
  <action>
    Create `tests/test_database.py` and implement focused tests against `src/article_factory/database.py` helpers.

    Required coverage:
    1. DB-01: create a topic with `create_topic`, then `get_topic` by returned ID, asserting topic/prompt/status fields are intact.
    2. DB-02: assert valid persisted transition paths using `update_status` and read-back checks:
       - NEW -> PENDING -> PROCESSING -> COMPLETED
       - PROCESSING -> FAILED
    3. DB-03: call `get_topic` with a non-existent ID and assert `None` is returned (no exception).

    Keep tests fixture-driven (no direct DB file I/O, no standalone engine creation in test file).
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_database.py -k "crud or transition or unknown" -v` exits 0.
  </verify>
  <done>
    `tests/test_database.py` includes passing assertions for CRUD retrieval, status transitions, and unknown-ID None behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add DB-04 concurrent async test and validate suite/coverage gates</name>
  <files>tests/test_database.py</files>
  <action>
    Extend `tests/test_database.py` with a concurrency-focused async test for DB-04.

    1. Use `pytest.mark.asyncio` and run multiple DB operations concurrently (e.g., `asyncio.gather` with `asyncio.to_thread` wrappers calling DB helper functions).
    2. Assert all operations complete successfully and no sqlite lock/operational errors are raised.
    3. Verify persisted outcomes by reading back created/updated records after concurrent execution.

    Then run full test and coverage commands for regression safety.
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/test_database.py -v` exits 0.
    2. `.venv/bin/python -m pytest tests/ --cov=src/article_factory --cov-report=term-missing -v` exits 0.
  </verify>
  <done>
    DB-04 is proven by a passing concurrent async test, and the full suite still passes under the enforced coverage threshold.
  </done>
</task>

</tasks>

<verification>
1. `tests/test_database.py` exists and covers DB-01..DB-04 explicitly.
2. All DB tests run through fixture-backed in-memory session patching (no real `article_factory.db` dependency).
3. Full suite and coverage commands still pass.
</verification>

<success_criteria>
- Topic CRUD retrieval is verified by tests with field-level assertions.
- Status transition persistence for completed and failed paths is verified.
- Unknown-topic lookup returns `None` without exception.
- Concurrent async DB operations complete in tests with no locking failures.
</success_criteria>

<output>
After completion, create `.planning/phases/06-database-errors-tests/06-01-SUMMARY.md`
</output>
