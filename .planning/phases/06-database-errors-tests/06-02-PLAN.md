---
phase: 06-database-errors-tests
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_errors.py
autonomous: true
must_haves:
  truths:
    - "rate_limiter prevents a fourth concurrent caller from entering while three slots are occupied"
    - "circuit_breaker opens after configured consecutive failures and rejects subsequent calls"
    - "circuit_breaker transitions to recovery state after cooldown and allows successful call flow again"
  artifacts:
    - path: "tests/test_errors.py"
      provides: "ERR-01..ERR-03 tests for RateLimiter and CircuitBreaker behavior"
      min_lines: 90
  key_links:
    - from: "tests/test_errors.py"
      to: "src/article_factory/errors.py"
      via: "direct RateLimiter and CircuitBreaker instantiation with test-specific thresholds"
      pattern: "RateLimiter|CircuitBreaker|CircuitOpenError"
    - from: "tests/test_errors.py"
      to: "asyncio"
      via: "concurrency orchestration and timeout assertions"
      pattern: "asyncio\\.(gather|wait_for|sleep|Event)"
---

<objective>
Add isolated resilience tests for `errors.py` verifying rate limiter concurrency blocking and circuit breaker open/recovery behavior.

Purpose: Phase 6 requires deterministic proof that resilience primitives enforce limits and recover correctly under async execution.
Output: `tests/test_errors.py` with ERR-01, ERR-02, and ERR-03 coverage and passing async assertions.
</objective>

<execution_context>
@/Users/giangnguyen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/giangnguyen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-layer/02-research-SUMMARY.md
@.planning/phases/05-test-infrastructure/05-01-SUMMARY.md
@src/article_factory/errors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ERR-01 rate limiter concurrency-blocking test</name>
  <files>tests/test_errors.py</files>
  <action>
    Create `tests/test_errors.py` and add async tests for `RateLimiter` with deterministic timing.

    1. Instantiate a local `RateLimiter(max_concurrent=3, min_interval=0)` in the test (do not rely on module global instance).
    2. Start three worker coroutines that acquire and hold slots until an `asyncio.Event` is released.
    3. Attempt a fourth acquire and assert it blocks while slots are occupied (e.g., `asyncio.wait_for(..., timeout=short)` raises timeout).
    4. Release held workers and assert the fourth caller can then acquire.

    Why: ERR-01 is specifically about concurrent slot limits, not interval delays.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_errors.py -k "rate_limiter" -v` exits 0.
  </verify>
  <done>
    A passing test proves the fourth concurrent caller is blocked until one of three held slots is released.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ERR-02 and ERR-03 circuit breaker open and cooldown recovery tests</name>
  <files>tests/test_errors.py</files>
  <action>
    Extend `tests/test_errors.py` with circuit breaker state tests using short thresholds/timeouts.

    1. Instantiate `CircuitBreaker(failure_threshold=2, recovery_timeout=<short>)` to keep tests fast.
    2. Provide an async function that raises to trigger failures; after threshold failures, assert state is OPEN.
    3. Assert subsequent call raises `CircuitOpenError` immediately while OPEN (ERR-02).
    4. Wait slightly longer than `recovery_timeout`, assert transition to HALF_OPEN, then run a successful async function and assert breaker closes/reset behavior (ERR-03).

    Do not modify runtime breaker defaults in `src/article_factory/errors.py`; use per-test instances only.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_errors.py -k "circuit_breaker or circuit" -v` exits 0.
  </verify>
  <done>
    Passing tests prove breaker opens after threshold failures, blocks calls while open, and recovers after cooldown with a successful probe.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full resilience and regression test validation</name>
  <files>tests/test_errors.py</files>
  <action>
    Run resilience-focused and full-suite checks after adding error tests.

    1. Run focused module tests to validate deterministic behavior.
    2. Run full `tests/` suite to confirm no cross-test regressions.
    3. Run coverage command to ensure phase additions remain compatible with fail-under gate.

    Keep the test run fully offline and fixture-safe (no real API calls).
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/test_errors.py -v` exits 0.
    2. `.venv/bin/python -m pytest tests/ -v` exits 0.
    3. `.venv/bin/python -m pytest tests/ --cov=src/article_factory --cov-report=term-missing -v` exits 0.
  </verify>
  <done>
    Errors module tests are stable and all existing tests continue passing with coverage enforcement enabled.
  </done>
</task>

</tasks>

<verification>
1. `tests/test_errors.py` exists and contains ERR-01..ERR-03 scenarios.
2. Rate limiter test demonstrates blocked fourth caller under three held permits.
3. Circuit breaker tests demonstrate OPEN rejection and cooldown recovery path.
4. Full test suite and coverage gate remain passing.
</verification>

<success_criteria>
- Rate limiter behavior is verified for concurrent-slot enforcement.
- Circuit breaker open-state rejection and recovery reset are both verified.
- Test suite remains fully offline and green under coverage constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/06-database-errors-tests/06-02-SUMMARY.md`
</output>
