---
phase: 08-content-wrapper-tests
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/article_factory/notebook_lm.py
  - tests/test_notebook_lm.py
autonomous: true
requirements:
  - NLM-01
  - NLM-02
  - NLM-03
  - NLM-04
  - NLM-05
must_haves:
  truths:
    - "Wrapper infographic generation deletes stale FAILED infographic artifacts before requesting a new one"
    - "Wrapper identifies the new infographic artifact using before/after artifact list diff"
    - "Wrapper polls raw artifact status until COMPLETED and returns the new artifact task_id"
    - "Wrapper raises RuntimeError when the new artifact transitions to FAILED"
    - "Wrapper raises RuntimeError when polling exceeds timeout without terminal completion"
  artifacts:
    - path: "tests/test_notebook_lm.py"
      provides: "NLM-01..NLM-05 deterministic wrapper coverage for cleanup, diffing, polling, failure, timeout"
      min_lines: 140
    - path: "src/article_factory/notebook_lm.py"
      provides: "Infographic wrapper behavior for artifact cleanup/diff/poll terminal handling"
      contains: "async def generate_infographic"
  key_links:
    - from: "tests/test_notebook_lm.py"
      to: "src/article_factory/notebook_lm.py"
      via: "direct invocation of NotebookLMClientWrapper.generate_infographic with mocked async client"
      pattern: "generate_infographic"
    - from: "src/article_factory/notebook_lm.py"
      to: "notebooklm._artifacts.ArtifactStatus"
      via: "status comparisons for COMPLETED and FAILED terminal polling outcomes"
      pattern: "ArtifactStatus\.COMPLETED|ArtifactStatus\.FAILED"
    - from: "src/article_factory/notebook_lm.py"
      to: "client.artifacts._list_raw"
      via: "before/after diff and polling loop state checks"
      pattern: "_list_raw"
---

<objective>
Add comprehensive offline tests for NotebookLM infographic wrapper internals (cleanup, artifact diffing, polling terminal states, and timeout behavior), with only minimal wrapper edits if tests reveal contract gaps.

Purpose: The wrapper is the critical safety boundary around unstable SDK behavior; Phase 8 must prove it is correct and deterministic under mocked artifact-list transitions.
Output: New `tests/test_notebook_lm.py` covering NLM-01..NLM-05 and targeted `notebook_lm.py` fixes only if required by those tests.
</objective>

<execution_context>
@/Users/giangnguyen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/giangnguyen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-delivery/03-media-SUMMARY.md
@.planning/phases/05-test-infrastructure/05-02-SUMMARY.md
@src/article_factory/notebook_lm.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NLM-01 and NLM-02 tests for failed-artifact cleanup and new-artifact diff detection</name>
  <files>tests/test_notebook_lm.py</files>
  <action>
    Create `tests/test_notebook_lm.py` with deterministic async tests for pre-trigger cleanup and diff logic.

    1. Build a mocked async wrapper client/context where `artifacts._list_raw` first returns a mix of FAILED + COMPLETED infographic entries, then a post-trigger list with a newly introduced infographic artifact ID.
    2. Assert `generate_infographic` deletes each FAILED infographic artifact before triggering generation (NLM-01).
    3. Assert wrapper identifies and tracks the new artifact via before/after diff (ID absent from `completed_before`) and does not reuse prior completed artifacts (NLM-02).
    4. Keep test fully offline: monkeypatch wrapper `get_client`, patch internal sleeps to no-op, and avoid any real SDK/network calls.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_notebook_lm.py -k "nlm_01 or nlm_02 or cleanup or diff" -v` exits 0.
  </verify>
  <done>
    Passing tests prove cleanup and before/after artifact diff behavior for wrapper infographic generation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add NLM-03 and NLM-04 tests for polling completion and failed terminal status</name>
  <files>tests/test_notebook_lm.py</files>
  <action>
    Extend wrapper tests to cover terminal poll outcomes.

    1. Add NLM-03 test where polling `_list_raw` transitions pending -> COMPLETED for the new artifact, then assert returned payload includes `{"task_id": new_id}`.
    2. Add NLM-04 test where polling shows `ArtifactStatus.FAILED` for the new artifact and assert `RuntimeError` is raised with failure messaging.
    3. Validate call flow still includes one trigger call to `artifacts.generate_infographic` and repeated `_list_raw` polling checks.

    Use deterministic side-effect lists and AsyncMock assertions for all artifact operations.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_notebook_lm.py -k "nlm_03 or nlm_04 or completed or failed" -v` exits 0.
  </verify>
  <done>
    Wrapper tests pass for both successful completion return contract and explicit failed-status exception path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add NLM-05 timeout test, patch wrapper only if needed, and run full wrapper suite</name>
  <files>src/article_factory/notebook_lm.py, tests/test_notebook_lm.py</files>
  <action>
    Add timeout-path coverage and apply minimal wrapper updates only if required for deterministic requirement compliance.

    1. Add NLM-05 test where polling never reaches COMPLETED/FAILED and assert `RuntimeError` timeout is raised after loop exhaustion.
    2. Patch internal sleeps (`asyncio.sleep`) to no-op so timeout behavior is fast and deterministic in unit tests.
    3. If current implementation behavior diverges from requirement assertions (cleanup, diff, terminal checks, timeout), make the smallest `src/article_factory/notebook_lm.py` changes needed to satisfy tests without changing unrelated wrapper APIs.
    4. Run full wrapper test module to confirm NLM-01..NLM-05 all pass together.
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/test_notebook_lm.py -k "nlm_05 or timeout" -v` exits 0.
    2. `.venv/bin/python -m pytest tests/test_notebook_lm.py -v` exits 0.
  </verify>
  <done>
    Timeout behavior is explicitly covered and full wrapper infographic requirement set NLM-01..NLM-05 is green.
  </done>
</task>

</tasks>

<verification>
1. `tests/test_notebook_lm.py` exists with explicit NLM-01..NLM-05 mapping.
2. Cleanup, diff detection, completed polling return, failed polling error, and timeout error are each independently asserted.
3. Tests remain fully mocked and credential-free, following Phase 5 offline policy.
4. Any `notebook_lm.py` edits are minimal and directly driven by failing wrapper tests.
</verification>

<success_criteria>
- Wrapper infographic behavior is deterministically verified across all required terminal paths.
- NLM-01..NLM-05 each have direct passing tests.
- No live NotebookLM calls are needed for verification.
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-wrapper-tests/08-02-SUMMARY.md`
</output>
