---
phase: 08-content-wrapper-tests
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_article_generation.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "CONTENT helper coverage artifact is substantive and satisfies the declared minimum-size gate"
    - "Additional CONTENT helper assertions are deterministic and keep phase-8 helper behavior green"
  artifacts:
    - path: "tests/test_article_generation.py"
      provides: "Expanded deterministic CONTENT helper assertions beyond the existing baseline"
      min_lines: 80
  key_links:
    - from: "tests/test_article_generation.py"
      to: "src/article_factory/article.py"
      via: "direct helper assertions for safety/citation/length/default-format contracts"
      pattern: "apply_safety_constraints|enforce_source_citations|validate_article_length|generate_article"
---

<objective>
Close the single Phase 8 verification gap by making `tests/test_article_generation.py` substantively larger than the plan threshold using deterministic CONTENT helper assertions.

Purpose: Phase 8 verification is blocked only by the declared artifact gate (78 < 80 lines), so this plan must add meaningful helper coverage rather than placeholder padding.
Output: Updated `tests/test_article_generation.py` with at least two additional deterministic helper assertions, plus green targeted verification.
</objective>

<execution_context>
@/Users/giangnguyen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/giangnguyen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-content-wrapper-tests/08-VERIFICATION.md
@.planning/phases/08-content-wrapper-tests/08-01-SUMMARY.md
@tests/test_article_generation.py
@src/article_factory/article.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add substantive deterministic CONTENT helper assertions to close min-lines gap</name>
  <files>tests/test_article_generation.py</files>
  <action>
    Extend `tests/test_article_generation.py` with at least two new deterministic assertions that exercise CONTENT helper behavior (not placeholder lines, comments, or duplicate trivial checks).

    Implementation constraints:
    1. Keep all additions offline and deterministic (no network/filesystem side effects).
    2. Prefer edge-case assertions around existing helper contracts (e.g., additional disallowed-pattern case, valid-citation pass-through case, or explicit in-range length acceptance) so coverage quality improves with the line-count fix.
    3. Preserve existing requirement mapping and naming style from 08-01 to keep traceability clear.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_article_generation.py -v` exits 0.
  </verify>
  <done>
    `tests/test_article_generation.py` includes at least two new non-placeholder deterministic helper assertions and total file length is at least 80 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run phase-8 content slice checks and confirm structural gap closure</name>
  <files>tests/test_article_generation.py</files>
  <action>
    Validate that the fix closes the exact verification gap without introducing regressions.

    1. Re-run the focused content helper module tests.
    2. Confirm file length threshold is met using a deterministic line-count check.
    3. Re-run the phase-8 targeted suite (`tests/test_article_generation.py`, `tests/test_media.py`, `tests/test_notebook_lm.py`) to ensure no collateral failures.
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/test_article_generation.py -v` exits 0.
    2. `.venv/bin/python -c "from pathlib import Path; print(sum(1 for _ in Path('tests/test_article_generation.py').open('r', encoding='utf-8')));"` prints a value `>= 80`.
    3. `.venv/bin/python -m pytest tests/test_article_generation.py tests/test_media.py tests/test_notebook_lm.py -v` exits 0.
  </verify>
  <done>
    Verification evidence shows the artifact gate is closed (`tests/test_article_generation.py` >= 80 lines) and all phase-8 test modules remain green.
  </done>
</task>

</tasks>

<verification>
1. `tests/test_article_generation.py` now meets or exceeds the declared 80-line threshold with meaningful assertions.
2. Added assertions validate real CONTENT helper behavior, not scaffolding.
3. Phase-8 content/wrapper test modules continue to pass after the change.
</verification>

<success_criteria>
- The only failed verification gap in `08-VERIFICATION.md` is directly resolved.
- `tests/test_article_generation.py` is substantive and no longer flagged as below the plan artifact gate.
- Phase 8 is ready for re-verification with no new gaps introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-wrapper-tests/08-03-SUMMARY.md`
</output>
