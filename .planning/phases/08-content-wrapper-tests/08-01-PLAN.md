---
phase: 08-content-wrapper-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/article_factory/article.py
  - src/article_factory/media.py
  - tests/test_article_generation.py
  - tests/test_media.py
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03
  - CONTENT-04
  - CONTENT-05
  - CONTENT-06
must_haves:
  truths:
    - "apply_safety_constraints rejects disallowed prompts with ValueError"
    - "enforce_source_citations rejects citations not present in available notebook sources"
    - "validate_article_length returns False when article is below minimum or above maximum word count"
    - "generate_article uses report format when caller does not provide format"
    - "get_output_dir resolves topic_name and date path correctly for both dict and ORM topic shapes"
    - "media.generate_infographic returns existing infographic path without triggering wrapper generation"
  artifacts:
    - path: "tests/test_article_generation.py"
      provides: "CONTENT-01..CONTENT-04 helper and default-format coverage"
      min_lines: 80
    - path: "tests/test_media.py"
      provides: "CONTENT-05 and CONTENT-06 coverage for output path resolution and infographic idempotency"
      min_lines: 80
    - path: "src/article_factory/media.py"
      provides: "Topic-shape-safe output directory resolution for dict and ORM topic objects"
      contains: "def get_output_dir"
  key_links:
    - from: "tests/test_article_generation.py"
      to: "src/article_factory/article.py"
      via: "direct helper-function calls and signature assertions"
      pattern: "apply_safety_constraints|enforce_source_citations|validate_article_length|generate_article"
    - from: "tests/test_media.py"
      to: "src/article_factory/media.py"
      via: "patched get_topic/os.path.exists checks through generate_infographic and get_output_dir"
      pattern: "get_output_dir|generate_infographic"
    - from: "src/article_factory/media.py"
      to: "src/article_factory/database.py"
      via: "get_topic lookup consumed by output directory and infographic paths"
      pattern: "get_topic"
---

<objective>
Add deterministic offline tests for content generation helpers and media idempotency behavior, with minimal source fixes where helper behavior does not satisfy requirement contracts.

Purpose: Phase 8 must prove article safety/citation/length rules and media output path behavior are reliable before wrapper-level infographic internals are validated.
Output: Expanded `tests/test_article_generation.py`, new `tests/test_media.py`, and any targeted updates in `article.py` / `media.py` needed to make CONTENT-01..CONTENT-06 pass.
</objective>

<execution_context>
@/Users/giangnguyen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/giangnguyen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-delivery/03-article-SUMMARY.md
@.planning/phases/05-test-infrastructure/05-02-SUMMARY.md
@src/article_factory/article.py
@src/article_factory/media.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONTENT-01..CONTENT-04 tests for article helper contracts</name>
  <files>tests/test_article_generation.py</files>
  <action>
    Expand `tests/test_article_generation.py` with explicit requirement-mapped tests for helper behavior in `article.py`.

    1. Add CONTENT-01 test(s) proving `apply_safety_constraints` raises `ValueError` when prompt text matches one of `DISALLOWED_PATTERNS`.
    2. Add CONTENT-02 test(s) proving `enforce_source_citations` raises `ValueError` when article citations include IDs/URLs not present in `available_sources`.
    3. Add CONTENT-03 test(s) proving `validate_article_length` returns `False` for below-minimum and above-maximum word counts.
    4. Add CONTENT-04 test proving `generate_article` defaults `format` to `"report"` when omitted (signature/default assertion is acceptable and deterministic).

    Keep tests pure/offline: no NotebookLM API calls, no real filesystem/network dependencies.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_article_generation.py -k "content or safety or citation or length or format" -v` exits 0.
  </verify>
  <done>
    `tests/test_article_generation.py` contains passing tests mapped to CONTENT-01, CONTENT-02, CONTENT-03, and CONTENT-04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CONTENT-05 and CONTENT-06 tests for media output resolution and idempotent infographic generation</name>
  <files>tests/test_media.py</files>
  <action>
    Create `tests/test_media.py` with focused coverage for media behaviors tied to Phase 8 requirements.

    1. Add CONTENT-05 tests for `get_output_dir(topic_id)` with both topic shapes:
       - dict topic from DB helpers,
       - ORM-like object exposing `topic` and `created_at` attributes.
       Assert output path uses expected `output/YYYY-MM-DD__topic-slug` format and creates the directory.
    2. Add CONTENT-06 idempotency test for `generate_infographic(topic_id)` where `infographic.png` already exists.
       - Patch topic lookup/output directory as needed.
       - Patch `os.path.exists(image_path)` to return True.
       - Assert function returns existing path and does not call `NotebookLMClientWrapper.generate_infographic` or `download_infographic`.

    Reuse fixture and monkeypatch conventions established in Phases 5-7.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_media.py -k "content_05 or content_06 or output_dir or existing" -v` exits 0.
  </verify>
  <done>
    New media tests pass and explicitly prove path-shape compatibility plus infographic idempotent early-return behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply minimal source fixes required by failing content/media tests and run full slice verification</name>
  <files>src/article_factory/article.py, src/article_factory/media.py, tests/test_article_generation.py, tests/test_media.py</files>
  <action>
    Run the new content/media test slice, then patch production code only where tests expose requirement gaps.

    1. If `get_output_dir` fails for ORM topic objects, update `src/article_factory/media.py` to safely resolve `topic` and `created_at` from both dict and attribute-based objects.
    2. If citation helper behavior is inconsistent with requirement assertions, apply minimal compatibility fixes in `src/article_factory/article.py` while preserving current source-only enforcement intent.
    3. Re-run both modules together to confirm CONTENT-01..CONTENT-06 pass as one coherent slice.

    Do not broaden scope beyond Phase 8 content/helper requirements.
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/test_article_generation.py tests/test_media.py -v` exits 0.
    2. `.venv/bin/python -m pytest tests/ --cov=src/article_factory --cov-report=term-missing -v` still exits 0.
  </verify>
  <done>
    Content/helper requirement tests pass together and any production edits are minimal, requirement-driven, and offline-safe.
  </done>
</task>

</tasks>

<verification>
1. CONTENT-01..CONTENT-04 are asserted in `tests/test_article_generation.py` with explicit ValueError/boolean/default checks.
2. CONTENT-05 and CONTENT-06 are asserted in `tests/test_media.py` for dict+ORM path compatibility and existing-file idempotency.
3. Any `article.py`/`media.py` edits are directly tied to failing tests and do not introduce live API behavior.
4. Combined content/media test run is green.
</verification>

<success_criteria>
- Content helper rules are demonstrably enforced by deterministic tests.
- Output directory behavior is correct across DB dict and ORM object topic representations.
- Existing infographic files short-circuit generation reliably with no wrapper calls.
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-wrapper-tests/08-01-SUMMARY.md`
</output>
