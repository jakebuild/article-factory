---
phase: 05-test-infrastructure
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tests/conftest.py
  - tests/test_article_generation.py
  - tests/test_models.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "API-touching tests are forced through shared NotebookLM mock fixtures; no real HTTP path is used"
    - "Each test run gets isolated in-memory SQLite setup via fixture wiring"
    - "At least one test proves seeded topic rows are available from fixture-backed DB state"
    - "pytest and pytest --cov still pass after fixture rewiring"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Autouse fixture wiring for NotebookLM mock path and in-memory DB isolation"
      contains: "@pytest.fixture(autouse=True)"
    - path: "tests/test_article_generation.py"
      provides: "Article generation tests consuming shared fixtures instead of ad hoc wrapper patching"
      contains: "mock_nlm_client"
    - path: "tests/test_models.py"
      provides: "Assertion that seeded topic rows exist through db_session fixture"
      contains: "db_session"
  key_links:
    - from: "tests/conftest.py"
      to: "article_factory.article.NotebookLMClientWrapper.get_client"
      via: "autouse monkeypatch/patch using mock_nlm_client"
      pattern: "get_client"
    - from: "tests/test_article_generation.py"
      to: "tests/conftest.py"
      via: "fixture arguments"
      pattern: "def test_.*\(.*mock_nlm_client"
    - from: "tests/test_models.py"
      to: "tests/conftest.py"
      via: "db_session fixture argument and seeded-row assertion"
      pattern: "def test_.*\(.*db_session"
---

<objective>
Close the two failed Phase 5 infrastructure truths by wiring existing shared fixtures into active tests and enforcing fixture-driven no-network + in-memory DB behavior.

Purpose: Phase 6-8 tests depend on reliable fixture plumbing; gaps show fixtures exist but are currently orphaned from test execution.
Output: Updated conftest/test files proving suite-wide mock + DB isolation behavior while preserving passing coverage gate.
</objective>

<execution_context>
@/Users/giangnguyen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/giangnguyen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-infrastructure/05-VERIFICATION.md
@.planning/phases/05-test-infrastructure/05-01-SUMMARY.md
@tests/conftest.py
@tests/test_article_generation.py
@tests/test_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce fixture-level mock and DB isolation wiring in conftest</name>
  <files>tests/conftest.py</files>
  <action>
    Implement suite-wide fixture wiring so the shared fixtures are always active where Phase 5 requires it.

    1. Keep the existing `mock_nlm_client` and `db_session` fixtures as the single source of truth.
    2. Add an autouse fixture that routes `NotebookLMClientWrapper.get_client` through `mock_nlm_client` so API-touching code paths cannot bypass shared fixture mocking.
    3. Ensure DB isolation is fixture-driven per test (either by making `db_session` autouse or by adding a targeted autouse fixture that depends on `db_session` and keeps current behavior).
    4. Preserve the existing monkeypatching decision from Phase 05: patch database globals (`_engine`, `SessionLocal`) to in-memory SQLite.

    Do not introduce any real network-blocking dependency; enforce isolation through pytest fixture wiring only.
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/ --collect-only -q` succeeds and shows no fixture import/collection errors.
    2. `tests/conftest.py` contains at least one `@pytest.fixture(autouse=True)` fixture tied to shared mock/DB path.
  </verify>
  <done>
    Shared fixtures are no longer orphaned: NotebookLM client mocking and in-memory DB setup are automatically activated through conftest-level fixture wiring.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace ad hoc test patching with shared fixture consumption</name>
  <files>tests/test_article_generation.py</files>
  <action>
    Refactor article-generation tests to consume shared fixtures from `tests/conftest.py` instead of local one-off patching.

    1. Remove local `patch("article_factory.article.NotebookLMClientWrapper.get_client", ...)` setup.
    2. Request `mock_nlm_client` explicitly in API-touching test signatures (and `db_session` if needed for Phase 5 isolation consistency).
    3. Keep behavioral assertions focused on generated content and artifact workflow; add at least one assertion that confirms the shared mock fixture object was used (e.g., awaited wrapper methods on `mock_nlm_client`).

    Follow the locked decision: no real API calls in tests; all SDK behavior must come from fixture mocks.
  </action>
  <verify>
    `.venv/bin/python -m pytest tests/test_article_generation.py -v` passes and no local wrapper patch block remains in the test file.
  </verify>
  <done>
    `test_article_generation.py` is fixture-driven (not ad hoc patched), and still passes with the same user-visible behavior checks.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove seeded DB availability and re-verify coverage gate</name>
  <files>tests/test_models.py</files>
  <action>
    Add or adjust model test coverage to explicitly verify seeded rows from the fixture-backed in-memory DB path.

    1. Add a test that requests `db_session` and asserts at least one seeded `Topic` row exists (prefer asserting known seed values like "Python Async" or "AI Safety").
    2. Keep existing pure model behavior tests intact.
    3. Run full suite and coverage command after rewiring to ensure infrastructure guarantees remain true.
  </action>
  <verify>
    1. `.venv/bin/python -m pytest tests/ -v` exits 0.
    2. `.venv/bin/python -m pytest tests/ --cov=src/article_factory --cov-report=term-missing -v` exits 0 with enforced `fail_under=70`.
  </verify>
  <done>
    At least one passing test demonstrates fixture-seeded topics from in-memory DB, and the full suite still passes with coverage gate enforced.
  </done>
</task>

</tasks>

<verification>
1. Confirm `tests/conftest.py` autouse fixture wiring is present for shared mocking and DB isolation.
2. Confirm `tests/test_article_generation.py` consumes shared fixture(s) and no longer relies on ad hoc wrapper patching.
3. Confirm `tests/test_models.py` includes seeded-topic assertion via `db_session`.
4. Run full test and coverage commands to verify no regression in pass/fail or fail-under gate.
</verification>

<success_criteria>
- Phase 5 gap truth for fixture-driven NotebookLM mocking is satisfied by active test wiring.
- Phase 5 gap truth for isolated seeded in-memory DB per test is satisfied and directly asserted.
- `pytest` and `pytest --cov` continue to pass with no NotebookLM credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-infrastructure/05-02-SUMMARY.md`
</output>
